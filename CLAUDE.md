# CLAUDE.md — 실패없는 카페 선택 (cafe-seat-mvp)

> 도착하기 전에 어느 카페에 자리가 있는지 알려주는 서비스.
> React · Vite · Firebase Firestore · GitHub Actions · Naver Maps API · Python.

**개발은 2026-06-19에 끝났다.** 기말 성과 분석으로 가설 검정까지 마치고 매듭지었고,
서버 없는 구조라 서비스는 지금도 돌아간다. 추가 개발 계획은 없다 —
이 저장소에서 하는 일은 대부분 아래 `.project.json` 문안 정리다.

## `.project.json` 작성 규칙 (허브 사이트 계약, 2026-08-13)

이 파일은 **허브 사이트**(`deukm0/project-hub` → https://deukm0-project-hub.vercel.app)가
GitHub API로 읽어 그대로 렌더한다. **원본은 여기이고 허브는 읽기만 한다** — 내용이 틀리면
허브가 아니라 이 파일을 고친다. 반영은 최대 1시간(ISR + 데이터 캐시).

### 절대 규칙

- **필드를 추가·변경·삭제하려면 사용자 승인이 먼저다.** 스키마는 7개 저장소가 공유하는
  계약이고 정본은 허브의 `docs/PROJECT_JSON_SCHEMA.md`다.
- **`status`는 다섯 값뿐** — `running` · `building` · `design` · `paused` · `archived`.
  없는 값을 쓰면 검증에 걸려 **이 프로젝트만 조용히 옛 데이터로 굳는다**(사이트는 안 깨진다).
- **`verdict`는 `archived`에 필수, `building`·`design`에는 금지.** 아직 낼 결론이 없는 단계다.
- **`period.end`는 "개발을 멈춘 날"이지 "서비스가 죽은 날"이 아니다.** 이 값이 있으면
  **그 뒤의 커밋은 활동 시간 집계에서 빠진다** — 허브에 등록하느라 이 파일을 고친 커밋이
  개발 시간으로 잡히던 것을 막기 위해서다.
- **비공개 저장소는 `links.repo`를 `null`로 둔다.** 링크 버튼만 사라지고 카드는 정상이다.
- **사실만 쓴다.** 계획을 완료형으로 쓰지 않는다. 안 만든 기능을 만든 것처럼 쓰지 않는다.
  근거 없는 수치·성과·사용자는 필드째 생략한다.

### 문장 규칙 — 실제로 여러 번 사고가 났던 것들

- **내부 식별자를 쓰지 않는다.** 스프린트 라벨(`4-3`), 빌드 UUID, 문서 번호(`00~15`),
  내부 신호 ID(`X-EV-01`), 내부 상태값(`M0_design_only`). 방문자에게 아무 정보도 주지 않는다.
  **새 문장을 쓸 때마다 다시 들어온다** — 쓰고 나서 한 번 훑을 것.
- **약어는 첫 등장에서 풀어 쓴다.** 상세 페이지는 위에서 아래로 읽히므로 페이지 **최하단**
  기술 개요에서 풀면 없는 것과 같다. 렌더 순서는 아래와 같다.

  - 제품: 문제 → 왜 만들었나 → 목표 → 해결 방식 → 사용 흐름 → 주요 화면 →
    **사용자 이득 → 결과와 검증 → 성취** → 판정 → 내 역할 → 의사결정 → 회고 → 기술 개요 → 스택
  - 연구: 왜 연구했나 → 연구 목표 → 연구 개요 → 연구 방법 → **연구 성과** → 한계 →
    의사결정 → 회고 → 판정 → 기술 개요 → 스택

- **문단은 빈 줄(`\n\n`)로 나눈다.** 한 줄로 이어 쓰면 화면에서 벽처럼 보인다.
  **스크립트로 넣을 때 이스케이프를 이중으로 씌우지 말 것** — 문자 그대로 `\n`이 화면에 뜬다.
- **`highlights`의 앞 3개는 홈 대표 카드에 그대로 나간다.** 처음 온 사람이 30초 안에 읽는
  자리이므로 **평문으로** 쓴다. 연구 프로젝트는 `product.benefits`·`outcomes`가 없어
  **반드시 `highlights`로 폴백**하므로 특히 그렇다.
- **`tagline`·`summary`는 한 줄이다.** 목록 카드는 요약만 담는다 — 깊은 내용은 상세가 전담한다.

### 고치고 나서

1. **push해야 허브가 읽는다.** 로컬 커밋만으로는 사이트에 반영되지 않는다.
2. 허브에서 `npm test`가 돌면 계약·문안·폰트 서브셋까지 함께 검증된다.
   새 한글이 들어오면 폰트 테스트가 잡는다(성능 문제일 뿐 글꼴은 폴백으로 맞는다).
